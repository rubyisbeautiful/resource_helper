require File.dirname(__FILE__) + '/../spec_helper'

describe "an ActionController with compatability for ActiveResource" do
  
  it "should fail because it has no spec" do
    true.should be_false
  end
  
end